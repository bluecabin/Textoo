ext {
    bintrayUser = 'userId'
    bintrayApiKey = "apiKey"
    bintrayGPGPassphrase = 'The passphrase for GPG signing'
    localMavenRepoPath = new File(System.getProperty('user.home'), 'Development/LocalMavenRepo').absolutePath
}